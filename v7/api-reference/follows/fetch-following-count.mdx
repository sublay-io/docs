---
title: "Fetch Following Count"
api: "GET /v7/:projectId/follows/following-count"
description: "Get the number of users the authenticated user follows"
---

Returns the total number of users that the authenticated user is following.

<Note>
  Users with a block between them and you, in either direction, are excluded
  from the count. Requires the `moderation` bundle.
</Note>

## Response

On success, returns HTTP `200`:

<ResponseField name="count" type="number">
  Total number of users the authenticated user follows.
</ResponseField>

## See Also

- [`useFetchFollowingCount` hook](/hooks/follows/use-fetch-following-count)
