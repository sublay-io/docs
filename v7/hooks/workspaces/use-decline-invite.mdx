---
title: "Decline invite"
description: "Decline a workspace invitation — identity-matched (not verification-gated)"
icon: "plug"
---

## Overview

`useDeclineInvite` returns a callable that declines an [invitation](/data-models/workspace-invitation). Identity-matched to the signed-in user, but **not** verification-gated.

## Usage Example

```tsx
import { useDeclineInvite } from "@sublay/react-js";

function DeclineButton({ inviteId }: { inviteId: string }) {
  const declineInvite = useDeclineInvite();

  return <button onClick={() => declineInvite({ inviteId })}>Decline</button>;
}
```

## Parameters

<ParamField path="inviteId" type="string" required>
  The invitation UUID.
</ParamField>

## Returns

`{ success: boolean }`.

## Related

- [Decline Invite API](/api-reference/workspaces/invitations/decline-invite)
